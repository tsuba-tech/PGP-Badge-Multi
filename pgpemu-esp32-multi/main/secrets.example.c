#include "secrets.h"

/* Copy to secrets.c and replace all three values with your own device data.
 * These placeholders deliberately prevent an accidental usable build. */
#error Replace placeholder MAC, DEVICE_KEY, and BLOB before building

uint8_t MAC[6] = {0};
uint8_t DEVICE_KEY[16] = {0};
uint8_t BLOB[256] = {0};
